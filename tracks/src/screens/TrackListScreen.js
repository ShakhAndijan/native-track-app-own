import React from 'react';
import { StyleSheet, Text } from 'react-native';

const TrackListScreen = () => {
  return <Text style={{ fontSize: 50 }}>TrackListScreen</Text>;
};

const styles = StyleSheet.create({});

export default TrackListScreen;
