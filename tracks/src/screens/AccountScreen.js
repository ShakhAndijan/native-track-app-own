import React from 'react';
import { StyleSheet, Text } from 'react-native';

const AccountScreen = () => {
  return <Text style={{ fontSize: 50 }}>AccountScreen</Text>;
};

const styles = StyleSheet.create({});

export default AccountScreen;
