import React from 'react';
import { StyleSheet, Text } from 'react-native';

const SignupScreen = () => {
  return <Text style={{ fontSize: 50 }}>SignupScreen</Text>;
};

const styles = StyleSheet.create({});

export default SignupScreen;
